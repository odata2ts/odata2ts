{
  "name": "@odata2ts/odata2ts",
  "version": "1.0.0",
  "private": true,
  "description": "Root project which allows for running single apps or deploy the whole stack.",
  "license": "MIT",
  "repository": "git@github.com:odata2ts/odata2ts.git",
  "scripts": {
    "build": "yarn clean && lerna run build",
    "clean": "rimraf coverage && lerna run clean",
    "coverage": "yarn workspace @odata2ts/odata2model dev && jest --coverage",
    "release": "yarn clean && yarn build && lerna publish",
    "test": "lerna run test",
    "int-test": "lerna run int-test"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^2.0.1",
    "husky": "^7.0.4",
    "jest": "^27.5.1",
    "lerna": "^4.0.0",
    "lint-staged": "^12.3.7",
    "prettier": "^2.6.2",
    "prettier-plugin-package": "^1.3.0",
    "prettier-plugin-properties": "^0.1.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{xml,js,jsx,ts,tsx,json,css,md}": [
      "prettier --write"
    ]
  },
  "workspaces": [
    "packages/*"
  ]
}
